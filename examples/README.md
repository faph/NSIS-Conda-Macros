Example NSIS source files for conda apps
========================================

This folder contains example NSIS source files for installing conda
applications.

Each installer can be built as follows:

```cmd
conda execute example.nsi
```
